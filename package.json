{
  "name": "itau",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify src/inicio.js -o dist/inicio.js & browserify src/dos.js -o dist/dos.js & browserify src/nuevotres.js -o dist/nuevotres.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "howler": "^2.1.2"
  },
  "devDependencies": {
    "browserify": "^16.5.0"
  }
}
